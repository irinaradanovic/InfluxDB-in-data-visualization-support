# InfluxDB-in-data-visualization-support

1. Please read instructions in README file in data/ directory
2. Copy the .env.example file and rename it to .env
3. Insert your credentials or leave the default values
4. docker-compose up --build -d in the terminal